const descriptionElement = document.querySelector('home-description');

descriptionElement.textContent = `Hi, I'm Jonatan. Full stack developer based in Stockholm, Sweden. I work with ClimateView via </salt>.`